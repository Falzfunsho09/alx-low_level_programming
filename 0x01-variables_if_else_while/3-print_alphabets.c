#include <stdio.h>
/**
 * main - Entry point
 * Return: 0 (Success)
 */

int main(void)
{
	char b;

	char m;

	b = 'a';
	m = 'A';
	while
		(b <= 'z') {
			putchar(b);
			b++;
		}
	while
		(m <= 'Z') {
			putchar(m);
			m++;
		}
	putchar('\n');
	return (0);
}
